server:  
  port: ${PORT:7741}

message: hola tio2

rop:
  bus:
    kafkaPortIp: kafkadockermastermulti_kafka_1:32768

#logging:
#  file: /logs/${spring.application.name}

eureka:
  client:
    region: default
    serviceUrl:
      defaultZone: http://discovery.ropapp.com:8761/eureka/
      #defaultZone: http://winhost.rop.com:8761/eureka/,http://winhost.rop.com:8762/eureka/
  instance:
    #prefer-ip-address: true
    hostname: dockerrop.testapp.com